action_endpoint:
  url: "http://localhost:5055/webhook"

# tracker_store:
#   type: sql
#   dialect: "sqlite"
#   db: "tracker.db"

# event_broker:
#   type: pika
#   url: localhost
#   username: username
#   password: password
#   queue: rasa_events
